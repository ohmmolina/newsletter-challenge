/*
  Routes that belong to the same group, but have different paths.
  Example: backoffice and backoffice-orders belong to the same group on the route '/backoffice', being '/backoffice' and '/backoffice/orders' their specific routes
*/
export default ['backoffice', 'auth']
